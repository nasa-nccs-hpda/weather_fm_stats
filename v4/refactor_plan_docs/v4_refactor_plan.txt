1. Lock down expected behavior first (Estimated LOC: 20-60) [Completed]
  - Confirm pipeline order: build source datasets once, then run regional and global stats as separate branches.
  - Confirm failure policy: if regional fails, global can still run (and vice versa), with clear error reporting.

2. Create one Python orchestrator flow (Estimated LOC: 120-260) [Completed]
  - Add one main path in v4 that runs the full workflow end-to-end inside one SLURM job.
  - Keep existing single-purpose flags for debugging and manual reruns.

3. Centralize runtime settings (Estimated LOC: 60-140) [Completed]
  - Put worker counts, chunk sizes, and resume behavior in one place (YAML + CLI overrides).
  - Keep defaults safe for your node size.

4. Parallelize source dataset creation in Python (Estimated LOC: 180-420) [Completed for forecast chunks]
  - Move heavy parallel work from SLURM job arrays into local worker processes.
  - Start with forecast chunk processing (biggest win), then collection-level parallel work.
  - Add clear wait points before merge stages.

5. Make init-date chunk planning and merge contracts deterministic (Estimated LOC: 120-260) [Completed]
  - Define one chunk plan format for forecast dataset chunks and future statistics chunks.
  - Each chunk must have a stable chunk index, inclusive start/end indices, selected init_dates, empty/skipped status, and expected output name.
  - Build chunk ranges from the full configured init-date sequence; record exclusions inside each chunk so skipped chunks are explicit.
  - Define merge order from the chunk plan, not from worker completion order or filename glob order.
  - Define required vs skipped chunks and the minimum validation needed before merge and cleanup.

6. Parallelize statistics calculation in Python (Estimated LOC: 200-420) [Completed]
  - Use the deterministic chunk plan to split regional and global statistics by init_dates.
  - Run statistics chunks with ProcessPoolExecutor inside the single pipeline job.
  - Write chunk outputs with expected deterministic names.
  - Merge statistics chunks automatically only after all required chunks finish successfully or are explicitly skipped.

7. Parallelize analysis and climatology dataset creation in Python (Estimated LOC: 260-520) [Completed]
  - Extend deterministic chunk planning beyond forecast init-date chunks.
  - Split analysis and climatology source dataset creation by deterministic valid-time chunks.
  - Run analysis and climatology chunks with local worker processes inside the single pipeline job.
  - Write deterministic ana/clim chunk output files and merge them in chunk-plan order.
  - Preserve final ana/clim filenames and existing validated-dataset skip behavior.

8. Strengthen serial regional and global branch independence (Estimated LOC: 100-220)
  - Keep regional and global stats as separate branches using the same fcst, ana, and clim inputs.
  - Keep regional and global branch execution serial in this refactor to avoid concurrent shared reads from the same source datasets.
  - Test and tune regional/global statistics worker counts and statistics chunk sizes to reduce total statistics execution time without adding branch-level concurrency.
  - Treat global statistics as the primary statistics bottleneck and use HPC timing tests to compare chunk-size and worker-count settings.
  - Record separate branch status, errors, outputs, and logs.
  - Allow one branch to fail without preventing the other from completing when fail policy permits.
  - Defer concurrent regional/global branch execution to a future refactor version after shared-read filesystem and memory behavior can be tested safely.

9. Add restart and resume support (Estimated LOC: 160-360) [Completed]
  - Reuse validated final source datasets and final regional/global statistics outputs in safe resume mode.
  - Report reused statistics branches explicitly with REUSED status and reuse reason.
  - Rebuild missing or invalid final outputs; do not trust partial old chunk outputs.
  - Defer full chunk-level checkpoint/resume to a later refactor if needed.

10. Improve logging for parallel processing (Estimated LOC: 80-180)
  - Reduce normal-case output verbosity so successful runs are easier to scan.
  - Do not print detailed validation output when validation passes normally; reserve details for warnings, failures, debug mode, or explicit verbose output.
  - Organize normal pipeline logs into clear forecast, analysis, climatology, regional statistics, and global statistics sections because these stages run serially.
  - Consolidate repeated normal-case messages within each section, including YAML initialization, batch forecast processing headers, target-grid creation, existing-dataset checks, and validation checks.
  - Ensure worker/chunk logs are explicitly labeled as chunk output and keep default chunk logging to at most five concise lines per chunk.
  - Avoid generic worker output that makes chunks look like full pipeline stages or standalone batch processors.
  - Add simple, readable progress messages for parallel dataset and statistics workers, including queued, started, completed, skipped, and failed chunks.
  - Include timing summaries and counts of completed, skipped, and failed chunks by dataset/statistics branch.
  - Record pipeline start time, finish time, and elapsed wall time in logs and run_summary.txt.
  - Improve CPU utilization reporting for parallel work by capturing child/worker process CPU usage where possible, so dataset and statistics chunk summaries reflect actual SLURM CPU use rather than only parent-process CPU time.
  - Improve child-process resource accounting for CPU and memory so summaries reflect actual ProcessPoolExecutor worker usage.
  - End with a concise final summary that highlights outputs, failures, warnings, and bottlenecks.

11. Extend resource-aware worker controls (Estimated LOC: 60-140)
  - Apply SLURM_CPUS_PER_TASK and configured limits consistently to dataset, statistics, and branch workers.
  - Clamp effective worker counts to the SLURM CPU allocation and required chunk count.
  - Surface the resolved worker cap in logs and summaries so clamping is visible during HPC runs.
  - Cap workers conservatively to avoid memory blowups.
  - Keep defaults safe for Discover node sizes.

12. Validate old vs new behavior with a small test (Estimated LOC: 100-240)
  - Use a small date range.
  - Run old flow and new flow separately.
  - Compare file presence, shapes and dimensions, and key stat values.
  - Goal: prove the refactor changed compute strategy, not scientific results.

13. Remove old multi-job assumptions (Estimated LOC: 80-220)
  - Delete or isolate legacy code that expects SLURM dependency chains.
  - Update docs and help text so single job plus in-process parallelism is the official model.

14. Improve v4 performance and optional accelerator usage (Estimated LOC: TBD)
  - Investigate why the v4 single-job workflow is not faster than the v1 multi-job workflow in Step 12 timing tests.
  - Profile dataset creation, merge, regional statistics, and global statistics phases to identify CPU, memory, I/O, and serialization bottlenecks.
  - Explore better CPU utilization strategies, including chunk-size tuning, worker-count tuning, process startup overhead reduction, and improved data loading/merge behavior.
  - Evaluate whether selected operations can optionally use GPU acceleration when requested in the YAML.
  - Add YAML-controlled accelerator settings only after there is a clear CPU/GPU execution path and a safe CPU fallback.
  - Preserve scientific output equivalence while improving wall-clock runtime.
