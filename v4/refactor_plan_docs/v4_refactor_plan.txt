1. Lock down expected behavior first (Estimated LOC: 20-60) [Completed]
  - Confirm pipeline order: build source datasets once, then run regional and global stats as separate branches.
  - Confirm failure policy: if regional fails, global can still run (and vice versa), with clear error reporting.

2. Create one Python orchestrator flow (Estimated LOC: 120-260) [Completed]
  - Add one main path in v4 that runs the full workflow end-to-end inside one SLURM job.
  - Keep existing single-purpose flags for debugging and manual reruns.

3. Centralize runtime settings (Estimated LOC: 60-140) [Completed]
  - Put worker counts, chunk sizes, and resume behavior in one place (YAML + CLI overrides).
  - Keep defaults safe for your node size.

4. Parallelize source dataset creation in Python (Estimated LOC: 180-420) [Completed for forecast chunks]
  - Move heavy parallel work from SLURM job arrays into local worker processes.
  - Start with forecast chunk processing (biggest win), then collection-level parallel work.
  - Add clear wait points before merge stages.

5. Make init-date chunk planning and merge contracts deterministic (Estimated LOC: 120-260) [Completed]
  - Define one chunk plan format for forecast dataset chunks and future statistics chunks.
  - Each chunk must have a stable chunk index, inclusive start/end indices, selected init_dates, empty/skipped status, and expected output name.
  - Build chunk ranges from the full configured init-date sequence; record exclusions inside each chunk so skipped chunks are explicit.
  - Define merge order from the chunk plan, not from worker completion order or filename glob order.
  - Define required vs skipped chunks and the minimum validation needed before merge and cleanup.

6. Parallelize statistics calculation in Python (Estimated LOC: 200-420) [Small validation succeeded; larger validation in progress]
  - Use the deterministic chunk plan to split regional and global statistics by init_dates.
  - Run statistics chunks with ProcessPoolExecutor inside the single pipeline job.
  - Write chunk outputs with expected deterministic names.
  - Merge statistics chunks automatically only after all required chunks finish successfully or are explicitly skipped.

7. Parallelize analysis and climatology dataset creation in Python (Estimated LOC: 180-420)
  - Extend source dataset parallelism beyond forecast chunks.
  - Split analysis dataset processing by deterministic valid-time chunks.
  - Split climatology dataset processing by deterministic valid-time or cycle chunks.
  - Merge ana and clim chunk outputs in chunk-plan order, with validation before cleanup.
  - Keep final ana and clim dataset filenames unchanged.

8. Strengthen regional and global branch independence (Estimated LOC: 100-220)
  - Keep regional and global stats as separate branches using the same fcst, ana, and clim inputs.
  - Record separate branch status, errors, outputs, and logs.
  - Allow one branch to fail without preventing the other from completing when fail policy permits.

9. Add restart and resume support (Estimated LOC: 160-360)
  - Track stage, branch, and chunk completion.
  - On rerun, skip completed good outputs and continue from the first incomplete point.
  - Avoid redoing expensive steps unless required.

10. Improve logs and run summary (Estimated LOC: 80-180)
  - Add simple, readable stage and chunk progress messages.
  - Include timing summaries and counts of completed, skipped, and failed chunks.
  - End with a concise final summary.

11. Extend resource-aware worker controls (Estimated LOC: 60-140)
  - Apply SLURM_CPUS_PER_TASK and configured limits consistently to dataset, statistics, and branch workers.
  - Cap workers conservatively to avoid memory blowups.
  - Keep defaults safe for Discover node sizes.

12. Validate old vs new behavior with a small test (Estimated LOC: 100-240)
  - Use a small date range.
  - Run old flow and new flow separately.
  - Compare file presence, shapes and dimensions, and key stat values.
  - Goal: prove the refactor changed compute strategy, not scientific results.

13. Remove old multi-job assumptions (Estimated LOC: 80-220)
  - Delete or isolate legacy code that expects SLURM dependency chains.
  - Update docs and help text so single job plus in-process parallelism is the official model.
